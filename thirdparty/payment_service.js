const fs = require('fs');
const path = require('path');

module.exports.queue_transaction = async (transaction_details) => {
    return new Promise((resolve, reject) => {
        resolve = true;
    });
}

module.exports.get_transaction_details = async (transaction_id) => {
    return new Promise((resolve, reject) => {
        resolve = true;
    });
}
